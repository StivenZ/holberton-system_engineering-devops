#!/usr/bin/env bash
# run container as nginx user
useradd nginx
sed -i "0,/user/ {
    s/user/user nginx/
    }"  /etc/nginx/nginx.conf

sed -i "0,/listen [::]/ {
    s/80/80/
    }"  /etc/nginx/sites-enabled/default
service nginx restart
